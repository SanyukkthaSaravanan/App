# openai@6 declares a peer dependency on zod@^3.25 while this project pins
# zod@3.23.8. That mismatch makes a plain `npm install` fail with ERESOLVE
# (e.g. on Render). Accept the existing resolution — matches how deps were
# installed and validated locally.
legacy-peer-deps=true
